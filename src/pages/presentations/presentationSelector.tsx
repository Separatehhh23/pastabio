import React from 'react';
import layout from '../../app/layout';
import styles from './presentationSelector.component.scss';

// Call layout to got the global css varibles
layout;

function PresentacionSelector() {
    return (
        <div>
            <h1>Hello</h1>
        </div>
    )
}

export default PresentacionSelector;